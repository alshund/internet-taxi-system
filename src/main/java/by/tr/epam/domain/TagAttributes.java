package by.tr.epam.domain;

public enum TagAttributes {
    command,

    userSignIn,
    userSignUp,
    role,

    id,
    email,
    password,
    passwordHash,
    salt,

    firstName,
    lastName,
    phoneNumber
}
